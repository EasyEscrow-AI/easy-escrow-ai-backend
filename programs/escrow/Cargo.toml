[package]
name = "escrow"
version = "0.1.0"
description = "Solana Escrow Program for NFT and USDC transactions"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "escrow"

[features]
default = []  # No default environment - must be explicitly specified at build time
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

# Environment-specific features (mutually exclusive)
# Build with: anchor build -- --features <environment>
mainnet = []
devnet = []
staging = []
localnet = []

# USDC support (disabled by default for legal compliance)
# Enable with: anchor build -- --features usdc
usdc = []

[dependencies]
anchor-lang = { version = "0.32.1", features = ["init-if-needed"] }
anchor-spl = "0.32.1"
solana-security-txt = "1.1.1"

