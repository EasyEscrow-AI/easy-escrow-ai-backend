{
  "name": "easyescrow-tests",
  "version": "0.1.0",
  "description": "Tests for EasyEscrow Solana Program",
  "scripts": {
    "test": "mocha --timeout 30000 'tests/**/*.test.ts'",
    "test:unit": "mocha --timeout 10000 'tests/program.test.ts'",
    "test:integration": "mocha --timeout 60000 'tests/integration.test.ts'",
    "test:watch": "mocha --timeout 30000 --watch 'tests/**/*.test.ts'"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.29.0",
    "@solana/web3.js": "^1.87.6",
    "@solana/spl-token": "^0.3.9"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "chai": "^4.3.0",
    "mocha": "^9.2.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.9.0"
  }
}