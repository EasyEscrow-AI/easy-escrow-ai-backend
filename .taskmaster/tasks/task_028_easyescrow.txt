# Task ID: 28
# Title: Implement Agreement Query and Cancel Endpoints
# Status: pending
# Dependencies: 24
# Priority: medium
# Description: Create GET /v1/agreements/{id} to return status, balances, deadlines. Create POST /v1/agreements/{id}/cancel to trigger cancellation for expired agreements
# Details:


# Test Strategy:


# Subtasks:
## 1. Design Agreement Data Model and Database Schema [pending]
### Dependencies: None
### Description: Define the database schema and data structures for storing agreement information including status, balances, and deadlines
### Details:
Create database tables/collections to store agreement data with fields for agreement_id, status (active/expired/cancelled), buyer_balance, seller_balance, nft_locked_status, usdc_locked_status, expiry_deadline, created_at, updated_at. Define TypeScript interfaces and validation schemas.

## 2. Implement Agreement Repository Layer [pending]
### Dependencies: 28.1
### Description: Create database access layer with methods to query and update agreement records
### Details:
Implement repository class with methods: findAgreementById(), updateAgreementStatus(), getAgreementBalances(), checkExpiryStatus(). Include proper error handling for not found cases and database connection issues.

## 3. Create GET /v1/agreements/{id} Endpoint [pending]
### Dependencies: 28.2
### Description: Implement REST endpoint to retrieve agreement details including status, balances, and deadlines
### Details:
Create Express.js route handler that validates agreement ID parameter, calls repository to fetch agreement data, formats response with status, buyer/seller balances, NFT/USDC lock status, expiry deadline. Return 404 for non-existent agreements.

## 4. Implement Agreement Expiry Detection Logic [pending]
### Dependencies: 28.2
### Description: Create service to detect expired agreements and validate cancellation eligibility
### Details:
Implement service methods to check if agreement has passed expiry deadline, validate that agreement is in cancellable state (not already settled/cancelled), and determine if cancellation is authorized. Include timezone handling for deadline comparisons.

## 5. Create POST /v1/agreements/{id}/cancel Endpoint [pending]
### Dependencies: 28.3, 28.4
### Description: Implement REST endpoint to trigger cancellation of expired agreements
### Details:
Create Express.js route handler that validates agreement ID, checks expiry status using detection service, updates agreement status to cancelled, triggers any necessary cleanup operations (unlock assets, notify parties). Return appropriate HTTP status codes and error messages.

