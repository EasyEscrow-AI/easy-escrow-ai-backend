# Task ID: 30
# Title: Deploy cNFT-Enabled Program to Devnet and Verify
# Status: done
# Dependencies: 24, 28
# Priority: high
# Description: Deploy updated program with cNFT support to devnet, upload IDL, update backend IDL files, and verify cNFT swaps work end-to-end on staging.
# Details:
Build program with cargo build-sbf from programs/escrow/. Generate IDL with anchor idl build. Deploy to devnet with anchor upgrade using staging-deployer.json. Upload IDL with anchor idl upgrade. Copy IDL to src/generated/anchor/escrow-idl-staging.json. Commit and push IDL update. Test manually on /test page with real cNFT. Verify on Solscan. Run E2E test suite. Document any issues encountered.

# Test Strategy:

