# Task ID: 11
# Title: Update Configuration and Environment Management
# Status: pending
# Dependencies: 1
# Priority: high
# Description: Implement comprehensive configuration management system with type-safe environment variables for nonce pool settings, platform authority keypairs, treasury PDA configuration, fee parameters, cNFT indexer API settings, and critical local/staging/production program ID management with environment-based switching and Docker development support.
# Details:
Implement the configuration and environment management system with the following comprehensive approach:

1. **Environment Variables Structure**:
   - Add NONCE_POOL_SIZE, NONCE_POOL_REFILL_THRESHOLD, NONCE_POOL_ADVANCE_INTERVAL environment variables
   - Add PLATFORM_AUTHORITY_KEYPAIR_PATH or PLATFORM_AUTHORITY_PRIVATE_KEY for keypair configuration
   - Add TREASURY_PDA_SEED, TREASURY_PDA_BUMP for treasury account derivation
   - Add FEE_FLAT_AMOUNT_LAMPORTS, FEE_PERCENTAGE_BASIS_POINTS, FEE_MAX_AMOUNT_LAMPORTS for fee calculation
   - Add CNFT_INDEXER_API_URL, CNFT_INDEXER_API_KEY, CNFT_INDEXER_TIMEOUT_MS for compressed NFT indexing
   - **CRITICAL**: Add SOLANA_NETWORK (local/staging/mainnet-beta) for three-tier environment identification
   - **CRITICAL**: Add LOCAL_PROGRAM_ID for local Docker development with test validator
   - **CRITICAL**: Add STAGING_PROGRAM_ID (AvdX6LEkoAmP961QwNjAUNpiuDtiQjaiSw5wR5zb9Zei) for staging environment
   - **CRITICAL**: Add PRODUCTION_PROGRAM_ID (configured only after staging validation)
   - **CRITICAL**: Add CURRENT_PROGRAM_ID (dynamically set based on SOLANA_NETWORK)
   - **NEW**: Add DATABASE_URL for local Docker PostgreSQL connection
   - **NEW**: Add REDIS_URL for local Docker Redis connection
   - **NEW**: Add SOLANA_RPC_URL (http://localhost:8899 for local, staging/production URLs for respective environments)

2. **Configuration Schema and Validation**:
   - Create comprehensive Joi or Zod schema for all environment variables with proper types and constraints
   - Implement startup validation that fails fast if required configurations are missing or invalid
   - Add range validation for numeric values (fees, timeouts, pool sizes) with sensible min/max bounds
   - Validate keypair format and accessibility during startup
   - Ensure treasury PDA configuration produces valid Solana addresses
   - **NEW**: Validate SOLANA_NETWORK is one of allowed values (local/staging/mainnet-beta)
   - **NEW**: Validate LOCAL_PROGRAM_ID is present when SOLANA_NETWORK is 'local'
   - **NEW**: Validate STAGING_PROGRAM_ID matches expected value (AvdX6LEkoAmP961QwNjAUNpiuDtiQjaiSw5wR5zb9Zei)
   - **NEW**: Validate PRODUCTION_PROGRAM_ID is only set when SOLANA_NETWORK is mainnet-beta
   - **NEW**: Validate Docker service URLs (DATABASE_URL, REDIS_URL) for local environment
   - **NEW**: Implement program ID validation that ensures local development is primary workflow

3. **Constants.ts Updates**:
   - Add new ATOMIC_SWAP_PROGRAM_ID constant replacing old escrow program ID
   - Create configuration accessor functions: getNoncePoolConfig(), getFeeConfig(), getTreasuryConfig(), getCNFTIndexerConfig()
   - **NEW**: Add getProgramConfig() function that returns current program ID based on environment (local/staging/production)
   - **NEW**: Add getNetworkConfig() function for network-specific settings including RPC URLs
   - **NEW**: Add getDatabaseConfig() and getRedisConfig() functions for Docker service connections
   - Implement type-safe configuration interfaces: NoncePoolConfig, FeeConfig, TreasuryConfig, CNFTIndexerConfig
   - **NEW**: Add ProgramConfig, NetworkConfig, DatabaseConfig, and RedisConfig interfaces
   - Add configuration validation helpers and error types for better debugging
   - **NEW**: Add three-tier program ID switching logic with local-first development support

4. **Configuration Loading and Caching**:
   - Implement singleton configuration manager that loads and validates all settings once at startup
   - Create configuration refresh mechanism for non-critical settings that can be updated without restart
   - Add configuration logging (with sensitive data redaction) for debugging and monitoring
   - Implement configuration defaults for local Docker development environment
   - **NEW**: Add program ID validation at startup that supports local/staging/production workflow
   - **NEW**: Implement environment-based program ID switching with Docker development support
   - **NEW**: Add Docker service connection validation for local development environment

5. **Type Safety and Developer Experience**:
   - Export strongly-typed configuration objects with IntelliSense support
   - Create configuration builder pattern for local Docker development testing
   - Add configuration documentation with examples and valid ranges for all three environments
   - Implement configuration change detection and warnings for staging/production deployments
   - **NEW**: Add program ID configuration documentation with local/staging/production deployment guidelines
   - **NEW**: Create environment-specific configuration templates for local Docker, staging, and production
   - **NEW**: Add Docker development setup documentation with service configuration examples

# Test Strategy:
1. **Configuration Validation Testing**:
   - Test startup validation with missing required environment variables and verify application fails to start with clear error messages
   - Test invalid configuration values (negative fees, invalid URLs, malformed keypairs) and verify proper validation errors
   - Test configuration loading with valid environment variables and verify all settings are properly parsed and accessible
   - **NEW**: Test SOLANA_NETWORK validation with invalid values and verify proper error handling for all three environments
   - **NEW**: Test LOCAL_PROGRAM_ID validation for local development environment
   - **NEW**: Test STAGING_PROGRAM_ID validation with incorrect values and verify startup failure
   - **NEW**: Test PRODUCTION_PROGRAM_ID validation that prevents usage without proper staging verification
   - **NEW**: Test Docker service URL validation (DATABASE_URL, REDIS_URL) for local environment

2. **Type Safety and Accessor Testing**:
   - Test all configuration accessor functions return properly typed objects with expected values
   - Verify configuration interfaces match actual environment variable structure and types
   - Test configuration caching behavior and ensure values remain consistent across multiple accessor calls
   - **NEW**: Test getProgramConfig() returns correct program ID based on SOLANA_NETWORK setting (local/staging/production)
   - **NEW**: Test getNetworkConfig() returns appropriate network-specific configurations including RPC URLs
   - **NEW**: Test getDatabaseConfig() and getRedisConfig() return proper Docker service connection details

3. **Integration Testing**:
   - Test configuration integration with NoncePoolManager, FeeCalculator, and other services that depend on these settings
   - Verify treasury PDA derivation produces valid and consistent addresses using configuration values
   - Test cNFT indexer configuration integration with actual API calls (using test endpoints)
   - **NEW**: Test program ID switching integration with all backend services across three environments
   - **NEW**: Verify all services use CURRENT_PROGRAM_ID correctly based on environment (local/staging/production)
   - **NEW**: Test Docker service integration with local PostgreSQL and Redis containers

4. **Environment-Specific Testing**:
   - Test configuration loading in local Docker, staging, and production-like environments
   - Verify configuration defaults work properly for local Docker development scenarios
   - Test configuration refresh mechanism without requiring application restart
   - **NEW**: Test local environment configuration with LOCAL_PROGRAM_ID and Docker services
   - **NEW**: Test staging environment configuration with STAGING_PROGRAM_ID (AvdX6LEkoAmP961QwNjAUNpiuDtiQjaiSw5wR5zb9Zei)
   - **NEW**: Test environment-based program ID switching between local, staging, and production
   - **NEW**: Verify production program ID is only usable after staging validation
   - **NEW**: Test Docker container connectivity and service availability for local development

5. **Security and Validation Testing**:
   - Verify sensitive configuration values (private keys, API keys, database passwords) are properly redacted in logs
   - Test configuration validation edge cases including boundary values for fees and timeouts
   - Verify keypair loading and validation works with different keypair formats and file permissions
   - **NEW**: Test program ID security validation that prevents accidental production usage
   - **NEW**: Verify local development program ID validation prevents unauthorized program ID usage
   - **NEW**: Test Docker service security configurations and connection string validation
