# Task ID: 17
# Title: Set up Local Docker Development Environment for Atomic Swap System
# Status: pending
# Dependencies: 1, 11
# Priority: high
# Description: Configure comprehensive local Docker development environment using existing Docker Compose setup with PostgreSQL, Redis, backend services, local Solana test validator, and development scripts to enable fast iteration during atomic swap system development.
# Details:
Configure the local Docker development environment using the existing Docker infrastructure with the following components:

1. **Existing Docker Compose Integration**:
   - Use existing docker-compose.yml with PostgreSQL container on localhost:5432 (postgres/postgres credentials)
   - Utilize existing Redis container on localhost:6379 for caching and session management
   - Leverage existing backend service container on port 3000 with health check functionality
   - Use existing Docker scripts: npm run docker:start, docker:stop, docker:restart for service management

2. **Environment Configuration for Atomic Swaps**:
   - Update existing .env file with atomic swap specific configuration variables
   - Set SOLANA_NETWORK=local, SOLANA_RPC_URL=http://localhost:8899, SOLANA_WS_URL=ws://localhost:8900
   - Configure LOCAL_PROGRAM_ID placeholder that gets populated after local program deployment
   - Add atomic swap specific environment variables (fee configuration, treasury settings, nonce pool configuration)
   - Ensure DATABASE_URL points to existing PostgreSQL setup (postgresql://postgres:postgres@localhost:5432/atomic_swap_db)
   - Set REDIS_URL=redis://localhost:6379 for existing Redis integration

3. **Database Integration with Existing Setup**:
   - Use existing npm run db:migrate command for executing atomic swap database migrations
   - Configure migration runner to work with existing PostgreSQL container setup
   - Add seed data script with test users, sample NFT metadata, and development swap offers for testing
   - Ensure database connection works with existing Docker network configuration

4. **Local Solana Development Integration**:
   - Use existing localnet scripts: npm run localnet:start for starting solana-test-validator
   - Utilize npm run localnet:setup for local keypair generation and account setup
   - Use npm run localnet:reset for cleaning validator state between development sessions
   - Configure LOCAL_PROGRAM_ID environment variable integration with existing localnet scripts
   - Set up local program deployment integration with existing development workflow

5. **Development Workflow Integration**:
   - Integrate atomic swap development with existing hot reloading setup
   - Configure automatic program redeployment workflow using existing development scripts
   - Set up development-specific logging configuration for atomic swap components
   - Ensure proper integration between Docker services and local Solana validator

6. **Unified Development Scripts**:
   - Create unified startup script that uses existing npm run docker:start and npm run localnet:start
   - Add atomic swap specific initialization that works with existing infrastructure
   - Create reset script that uses existing docker:restart and localnet:reset commands
   - Integrate health check verification for both existing services and new atomic swap components

# Test Strategy:
1. **Existing Docker Environment Validation**:
   - Execute `npm run docker:start` and verify all existing services (PostgreSQL, Redis, backend) start successfully
   - Test service connectivity to PostgreSQL on localhost:5432 with postgres/postgres credentials
   - Verify Redis connectivity on localhost:6379 and backend service health check on port 3000
   - Confirm existing hot reloading functionality works with atomic swap code changes

2. **Local Solana Integration Testing**:
   - Execute `npm run localnet:start` and verify solana-test-validator starts with RPC at http://localhost:8899
   - Run `npm run localnet:setup` and confirm local keypair generation and account setup works correctly
   - Test LOCAL_PROGRAM_ID environment variable integration with existing localnet scripts
   - Verify `npm run localnet:reset` properly cleans validator state

3. **Database Migration Integration**:
   - Execute `npm run db:migrate` and verify atomic swap migrations run against existing PostgreSQL setup
   - Confirm database connection works with existing Docker network configuration
   - Test seed data insertion with existing database setup and verify proper data relationships

4. **Unified Development Workflow Testing**:
   - Test complete startup sequence using existing npm scripts (docker:start, localnet:start)
   - Verify atomic swap program deployment works with existing localnet infrastructure
   - Execute reset workflow using existing scripts (docker:restart, localnet:reset) and confirm clean state
   - Validate integration between existing Docker services and local Solana validator for atomic swap development

# Subtasks:
## 1. Update .env file with atomic swap configuration [pending]
### Dependencies: None
### Description: Update the existing .env file to include all atomic swap specific environment variables while preserving existing configuration for Docker and database setup.
### Details:
Modify the existing .env file to add atomic swap specific variables:
- SOLANA_NETWORK=local
- SOLANA_RPC_URL=http://localhost:8899
- SOLANA_WS_URL=ws://localhost:8900
- LOCAL_PROGRAM_ID (placeholder for local deployment)
- Fee configuration variables (FLAT_NFT_FEE, SOL_SWAP_PERCENTAGE, MAX_FEE_CAP_SOL)
- Treasury PDA configuration (TREASURY_PDA_SEED, TREASURY_PDA_BUMP)
- Nonce pool settings (NONCE_POOL_SIZE, NONCE_POOL_REFILL_THRESHOLD)
- Platform authority keypair configuration
Ensure existing DATABASE_URL and REDIS_URL remain compatible with current Docker setup.

## 2. Configure database migrations with existing npm scripts [pending]
### Dependencies: None
### Description: Ensure atomic swap database migrations work correctly with the existing npm run db:migrate command and Docker PostgreSQL setup.
### Details:
Configure the migration system to work with existing infrastructure:
- Verify npm run db:migrate works with atomic swap migrations from Task 1
- Ensure migrations run against existing PostgreSQL container (postgres/postgres@localhost:5432)
- Test migration rollback functionality with existing database setup
- Configure migration state tracking to work with existing database schema
- Add atomic swap specific seed data that integrates with existing development workflow

## 3. Integrate LOCAL_PROGRAM_ID with existing localnet scripts [pending]
### Dependencies: 17.1
### Description: Configure LOCAL_PROGRAM_ID environment variable to work seamlessly with existing npm run localnet scripts for program deployment and management.
### Details:
Integrate program ID management with existing localnet infrastructure:
- Modify existing localnet:setup script to capture and set LOCAL_PROGRAM_ID after program deployment
- Ensure LOCAL_PROGRAM_ID is properly updated in .env file during local development
- Configure program deployment to work with existing solana-test-validator setup
- Add LOCAL_PROGRAM_ID validation to existing health check scripts
- Ensure program ID persistence across localnet:reset operations when needed

## 4. Create unified development startup script [pending]
### Dependencies: 17.2, 17.3
### Description: Create a unified startup script that orchestrates existing Docker and localnet scripts for complete atomic swap development environment initialization.
### Details:
Develop a comprehensive startup script that:
- Executes npm run docker:start to initialize PostgreSQL, Redis, and backend services
- Waits for Docker service health checks to pass
- Runs npm run db:migrate to apply atomic swap database migrations
- Executes npm run localnet:start to start solana-test-validator
- Runs npm run localnet:setup for keypair and program deployment
- Verifies all services are running and properly configured
- Provides clear status feedback and error handling throughout the process

## 5. Create unified development reset script [pending]
### Dependencies: 17.4
### Description: Create a comprehensive reset script that uses existing Docker and localnet reset commands to provide clean development environment reinitialization.
### Details:
Develop a reset script that:
- Executes npm run docker:stop to stop all Docker services
- Runs npm run localnet:reset to clean Solana validator state
- Clears any cached program deployments and temporary files
- Executes npm run docker:restart to reinitialize Docker services with clean state
- Runs database migrations and seed data insertion for fresh development state
- Provides option to preserve or reset LOCAL_PROGRAM_ID based on development needs
- Includes verification steps to ensure clean environment reset

